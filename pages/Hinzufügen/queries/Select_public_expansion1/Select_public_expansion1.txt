SELECT
  *
FROM
  public.expansion
ORDER BY
  "{{Table1.sortOrder.column || 'expansion_id'}}" {{Table1.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{Table1.pageSize}}
OFFSET
  {{Table1.pageOffset}}